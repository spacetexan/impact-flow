# Storage backend: 'memory' | 'sqlite' | 'server' | 'supabase'
# - memory: In-memory only, data lost on refresh
# - sqlite: Browser-based SQLite with IndexedDB persistence
# - server: Express backend with file-based SQLite (recommended)
# - supabase: Cloud database (not yet implemented)
VITE_STORAGE_TYPE=server

# SQLite persistence mode (only applies when VITE_STORAGE_TYPE=sqlite)
# 'memory' = no persistence (resets on refresh)
# 'indexeddb' = persistent storage in browser IndexedDB
VITE_SQLITE_PERSISTENCE=indexeddb

# API URL for server mode (only applies when VITE_STORAGE_TYPE=server)
VITE_API_URL=http://localhost:3001/api

# Enable demo mode with sample data (true | false)
# When true, loads sample profiles/projects on first run
# Set to false for an empty database
VITE_DEMO_MODE=true

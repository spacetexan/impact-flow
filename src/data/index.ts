/**
 * Data layer barrel export
 */

export { seedProfiles, seedProjects, seedCriteria, allSeedData } from './seedData';
export type { SeedData } from './seedData';

export { getInitialData, seedRepositories, isDemoMode } from './seedLoader';
